Template.channelShow.onDestroyed(function() {
  // Session.set('currentChannel', null);
  // Session.set('currentChannelName', null);
  // Session.set('currentChannelCreator', null);
  // Session.set('currentChannelCreatorName', null);
  // Session.set('selectBuyer', null);
});

Template.channelShow.onCreated(function() {
  // Session.set('currentChannel', this.data._id);
  // Session.set('currentChannelName', this.data.name);
  // Session.set('currentChannelCreator', this.data.creator);
  // Session.set('currentChannelCreatorName', this.data.creatorName);
});
